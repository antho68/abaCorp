<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <!-- Change this folder if you want -->
        <Property name="log.path">C:\temp\abaCorp</Property>
        <Property name="app.name">app</Property>
    </Properties>

    <Appenders>
        <!-- Console (always useful during development) -->
        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- Daily rolling file – one file per day -->
        <RollingFile name="File" fileName="${log.path}/${app.name}.log"
                     filePattern="${log.path}/${app.name}-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1.} - %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="100 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

        <!-- Optional: separate file only for ERROR level -->
        <RollingFile name="ErrorFile" fileName="${log.path}/${app.name}-error.log"
                     filePattern="${log.path}/${app.name}-error-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1.} - %msg%n"/>
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
            <Policies>
                <TimeBasedTriggeringPolicy/>
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Your own packages – adjust to your real package -->
        <Logger name="com.aba.corp" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
            <AppenderRef ref="ErrorFile"/>
        </Logger>

        <!-- PrimeFaces, Mojarra, Weld noise reduction -->
        <Logger name="org.primefaces" level="warn"/>
        <Logger name="com.sun.faces" level="warn"/>
        <Logger name="jakarta.faces" level="warn"/>
        <Logger name="org.jboss.weld" level="warn"/>

        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </Root>
    </Loggers>
</Configuration>